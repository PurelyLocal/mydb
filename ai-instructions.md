# AI Instructions

- Tone: concise, operational, DevOps‑friendly.
- Always update schema/templates when adding new metadata.
- Do not include secrets in repo; rely on CI secrets.
- Keep `build.config.yaml` and `mkdocs.yml` in sync with folder structure.
- When changing audience rules, update `ACCESS_MODEL.md`.
