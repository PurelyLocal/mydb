# Content Operations

- Engineers create/maintain MI/PRB/Journal/OKR via templates.
- `scripts/build_indexes.py` injects dashboards into index pages.
- `scripts/pull_*.py` fetch external data to `docs/data/`.
- Bot PRs: periodic automation to refresh data; reviewers merge after checks.
